{
  "version": "independent",
  "npmClient": "yarn",
  "packages": ["packages/*"],
  "useWorkspaces": true,
  "command": {
    "publish": {
      "conventionalCommits": true,
      "message": "chore(release): publish",
      "registry": "http://localhost:4873",
      "allowBranch": "deploy-test",
      "ignoreChanges": ["*.md"]
    }
  }
}
